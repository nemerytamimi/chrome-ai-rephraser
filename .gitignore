# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local state (machine-specific; may contain local paths/permissions)
.claude/settings.local.json

# Node (if tooling is added later)
node_modules/
npm-debug.log*
yarn-error.log*
package-lock.json.bak

# Build / packaging output
dist/
build/
*.zip
*.crx
*.pem

# Local secrets — never commit these
.env
.env.*
!.env.example
secrets.json
config.local.json
